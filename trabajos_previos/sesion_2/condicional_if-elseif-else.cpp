int main(){
    int numero = 0;

    if (numero > 0) {
        // Aqui se ejecuta el codigo si condicion del if es verdadera.
    }
    else if (numero == 0){
        // Aqui se ejecuta el codigo si la primera condicion es falsa y la dada en el else if es verdadera
    }
    else {
        // Aqui se ejecuta el codigo si las condiciones eran falsas
    }
    // aqui se ejecuta el codigo despues de comprobar la condicion, si la condicion era falsa, se salta todo el codigo del if
}