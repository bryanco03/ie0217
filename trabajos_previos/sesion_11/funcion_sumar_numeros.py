def add_numbers(a = 7, b = 8):
    sum  =  a + b
    print("sum:", sum)

# llamando la funcion con dos argumentos
add_numbers(2, 3)

# Llamando la funcion con 1 argumento
add_numbers(a=2)

# Llamando la funcion sin agumentos
add_numbers()